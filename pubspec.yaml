name: ssid_finder
description: Extract SSID tokens from trading platforms with ease. Supports OlympTrade, PocketOptions, and more.
publish_to: 'none'
version: 1.0.0+1

# App metadata for stores
homepage: https://github.com/theshadow76/Chipa-SSID-Finder

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_inappwebview: ^6.0.0
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  provider: ^6.1.1
  window_manager: ^0.3.8
  fl_chart: ^0.66.0
  font_awesome_flutter: ^10.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
